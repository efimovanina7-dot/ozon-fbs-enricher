# Здесь будет тонкий клиент Ozon (httpx + аутентификация через заголовки).
# Функция get_posting(posting_number: str) -> dict вернёт ответ Ozon,
# а сервис превратит его в плоские поля по config/fields.yml.
